{
  "name": "scoreboard",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:app": "bun run --filter @scoreboard/app dev",
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter @scoreboard/display build && bun run --filter @scoreboard/app build && bun run --filter @scoreboard/server build",
    "start": "bun run --filter @scoreboard/server start",
    "test:e2e": "bun run build && bunx playwright test",
    "patchAndTag": "bash scripts/bump-and-tag.sh patch",
    "minorAndTag": "bash scripts/bump-and-tag.sh minor",
    "package": "bun run build && docker buildx build --platform linux/amd64,linux/arm64 --push -t europe-west1-docker.pkg.dev/uccle-sport/docker/scoreboard:$npm_package_version ."
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^22.16.5"
  }
}
